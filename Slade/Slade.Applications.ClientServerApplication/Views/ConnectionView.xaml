<UserControl x:Class="Slade.Applications.ClientServerApplication.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition Width="10" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>

		<!--  Username  -->
		<TextBlock Grid.Row="0"
		           Grid.Column="0"
		           Text="Username" />
		<TextBox Grid.Row="1"
		         Grid.Column="0"
		         IsEnabled="{Binding Path=ConnectionInformation.IsEditing}"
		         Text="{Binding Path=ConnectionInformation.Username,
		                        Mode=TwoWay}" />

		<!--  Connection Address  -->
		<TextBlock Grid.Row="0"
		           Grid.Column="2"
		           Text="Address" />
		<TextBox Grid.Row="1"
		         Grid.Column="2"
		         IsEnabled="{Binding Path=ConnectionInformation.IsEditing}"
		         Text="{Binding Path=ConnectionInformation.Address,
		                        Mode=TwoWay}" />

		<!--  Connection Instigation Button  -->
		<Button Grid.Row="2"
		        Grid.Column="0"
		        Grid.ColumnSpan="3"
		        HorizontalAlignment="Right"
		        Command="{Binding RefreshConnectionCommand}"
		        Content="Refresh Connection" />
	</Grid>
</UserControl>